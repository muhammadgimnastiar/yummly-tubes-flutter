import 'package:flutter/material.dart';

class Article {
  String id, title, image, content;

  Article({
    required this.id,
    required this.title,
    required this.image,
    required this.content,
  });
}
